#include <stdio.h>
/**
* main - write a program that print all the number of base 16
*
* Return: 0
*/

int main(void)
{
	int a;

	for (a = 0; a < 16; a++)
		printf("%d", a);
	putchar('\n');
	return (0);
}
