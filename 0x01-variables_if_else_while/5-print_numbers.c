#include <stdio.h>
/**
 * main - write a script that prints all single digit number of base 10
 *
 * Return: 0
 */

int main(void)
{
	int a;

	for (a = 0; a < 10; a++)
		printf("%d", a);
	putchar('\n');
	return (0);
}

